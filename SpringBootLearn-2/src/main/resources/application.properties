spring.datasource.url=jdbc:mysql://localhost:3306/springbootlearning
spring.datasource.username=root
spring.datasource.password=FlyingF!r3


#So the list of possible options are,
	#validate: validate the schema, makes no changes to the database.
	#update: update the schema.
	#create: creates the schema, destroying previous data.
	#create-drop: drop the schema when the SessionFactory is closed explicitly, typically when the application is stopped.
	#none: does nothing with the schema, makes no changes to the database
spring.jpa.hibernate.ddl-auto=update

# Responsible to identify the underlying database and generates the SQL queries.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#	Includes customized error messages in response
server.error.include-message = always